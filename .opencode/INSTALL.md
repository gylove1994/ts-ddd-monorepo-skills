# OpenCode Installation

Use this repository as an OpenCode plugin source.

The plugin entrypoint is:

```text
.opencode/plugins/skills-template.js
```

After installation, OpenCode should use `AGENTS.md` for repository guidance and `skills/` as the shared skill library.
