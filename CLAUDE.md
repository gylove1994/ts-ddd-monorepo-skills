# Claude Instructions

Use `AGENTS.md` as the source of truth for repository-level guidance.

When a user request matches a workflow described in `skills/`, read the matching `SKILL.md` before acting.
